# Reverse Specification Overview

> Generated by smith reverse (run_id: 20260717_143507_0f23e40d)
> Profile: generic
> Provider: claude-agent

---

## システム概要

Nissan向けと思われる運転診断モバイルアプリ(driving-score, appId=jp.co.nissan.drivingscore)。Ionic+Angular15+Capacitor4/Cordovaでハイブリッド構成し、Androidと開発検証用ブラウザの両方で動作する。運転者がスマートフォンをダッシュに置いて運転すると、GPS/加速度/ジャイロ/磁気センサー(および任意でBLE経由の車載CANデータ)を統合して動的JSスコアロジックが総合スコア/4指標スコア/能力指標(scoreA/B/C)を算出し、ヒヤリ地点を地図に記録、走行動画とセンサーログを紐付けて振り返り可能にする。

---

## 主要機能

- **運転者(ドライバー)**: アプリを起動する
- **運転者(ドライバー)**: オープニング画面でログインボタンを押しID/PASSWORDを入力する
- **運転者(ドライバー)**: アカウント作成ボタンから新規アカウントを登録する
- **運転者(ドライバー)**: 運転診断画面のスタートボタンで運転診断を開始する
- **運転者(ドライバー)**: 運転診断画面のストップボタンを押す、またはセンサー異常が検出される
- **運転者(ドライバー)**: 運転診断終了後にスコアパネルをタップしコメントページを開く
- **運転者(ドライバー)**: 地図上のヒヤリ地点マーカーをタップする
- **運転者(ドライバー)**: 履歴ボタンから運転診断履歴を確認する
- **検証担当者/開発者**: 設定ページからスコアロジック/設定JSONを差し替える
- **検証担当者/開発者**: 編集ページでセンサーログをアップロードしスコアロジックを再実行検証する

---

## 画面構成

openingPage(トップ/ログイン)、accountPage(アカウント作成/編集/削除, ルート引数type)、drivingPage(運転診断メイン、地図+スコア+動画録画)、commentPage(直近運転診断のアドバイス)、badSpotPage(ヒヤリ地点詳細、地図+動画同期再生, ルート引数path)、historyPage(履歴グラフと能力指標グラフ, タブ切替)、settingsPage(録画/ログ/センサーモード切替とスコアロジック更新)、editPage(センサーログをアップロードしスコアロジック検証+多系列グラフ)。

---

## データの流れ

スマホセンサー(Capacitor Motion/Geolocation/Magnetometer + BLE Notify)→ SensorService(10ms合成) → SensorManager.calibration(オフセット/ローパス/回転補正) → ScoreLogic(scoreLogicKeyの動的JSを300ms間隔で実行) → Score/Message/CapabilityScore → UI更新(Chart.js, Google Maps) → 診断終了時にScoreDbServiceがSQLiteへ永続化 → History/Comment/BadSpotで参照。設定/ロジックはLoginServiceのsettings + Ionic Storage KVSで共有。

---

## 永続化

1) SQLite(cordova-sqlite-storage, driving-score.db):テーブル users(user_id PK, user_password, sex, birth_year, birth_month, height, prefecture), score(score_id PK=timestamp, user_id, score_over_all, score1〜4), score_history(score_id, timestamp, message_id, message_key, message_type, message_text, intersection, score), capability_score(score_id, timestamp, score_a/b/c, score_a/b/c_message)。2) Ionic Storage KVS: scoreLogicKey/scoreLogicJsonKey/loginKey/lastLoginUserId/geolocationLastPosKey/setting-*。3) 端末外部ストレージ(Android Documents/driving-score/): 診断毎ディレクトリ配下にmovie.webm/log.YYYYMMDD-hhmmss.txt.gz(gzip)/sensor-log.YYYYMMDD-hhmmss.txt.gz/scoreLogicJson.txt/scoreLogic.txt。

---

## 外部依存

Google Maps JavaScript API(@googlemaps/js-api-loader, environment.mapsKey)、Google Maps SDK for Android(AndroidManifestのAPI_KEY)、BLE車載機'DrivingCanData'(UUID 0x2310/0x2311, 11byte固定フォーマット)、Capacitor4系(Geolocation, Motion, Bluetooth-le, App, Keyboard, Haptics, StatusBar, Google Maps)、Cordovaプラグイン(sqlite, device-motion, magnetometer, insomnia, screen-orientation, android-permissions, file)、Chart.js 4系、pako(gzip)、ts-md5。

---

## 認証

端末内ローカル認証。ID(英数字)+パスワード(ts-md5でMD5ハッシュ、salt無し)をSQLite usersテーブルと照合。ログイン成功時はloginKeyに(timestamp/userId/hashedPassword)を保存し、以後3日間は自動ログイン。サーバ側の認証やOAuthは無い。

---

## バックグラウンド処理

運転診断中にsetInterval(10ms)でセンサースナップショットを合成、setInterval(既定300ms)でScoreLogicを実行、MediaRecorderは60秒毎にdataavailableイベントを発火しwebmファイルへ追記。ログサービスはlog>=3MB/センサーログ>=5MBで自動的にgzipファイル出力(force指定時は即時)。BLEスキャンは3秒後にstopし単一デバイスなら自動接続、複数なら選択ダイアログ。編集ページのSensorTimerも同様に10ms駆動。

---

## 業務ルール

1) 自動ログインの有効期間は最後のログインから3日間。2) score/score1〜4は0-100、Message.scoreによる代表選出はpositive=最大、negative=最小。3) 表示は生スコアか順位(rank=101-score, clamp 100)を score_show_star.area1〜3 で切替。4) 履歴グラフの表示件数上限は30件(HistoryPage.GRAPH_MAX)、走行軌跡マーカーは1000件でFIFO。5) 能力指標の総合値は capability_score_target_days(既定30日)以内の平均。6) ScoreLogicJSON差替時はversion(scoreLogic.json 現行0.33)を比較し、既存より古いversionは無視。scoreLogicは1行目のUnixTime(//1740668400000)を比較し新しい方を採用、ブランクなら自動付与。7) 診断結果INSERTは1000件単位で分割。8) センサーデータ蓄積は最新60秒間(sensorStockTime)に制限し古いものはpurge。9) センサーキャリブレーションは開始から100msスキップ後の1000ms分の平均をオフセット化、時定数T=0.0167s/カットオフF=0.2Hzのローパスを適用。10) BLE Notifyの11バイトから vehicleSpeed(0-255)/longAcc(-1.28〜1.27)/latAcc/frontDistance/lateralDistance(-64〜63.5)/steeringAngle(-1080〜1080°)/accelPedalPosition/brakePressure/brakeSwitch/shiftIndication(P/R/N/D/L/B)/turnSignal(OFF/L/R/HAZARD)にデコード。11) センサーモード smartphoneOnly ではBLE接続処理を行わない。

---

## テスト観点

File breakdown by extension:
- .ts: 46 files
- .json: 8 files
- .bat: 4 files
- .sh: 3 files
- .js: 2 files
- .txt: 2 files
- .xml: 1 files
- .xlsx: 1 files

---

## Reverse の限界

1) *.page.htmlと*.scssを未読(コンポーネントロジックのみで画面レイアウト詳細は推測)。2) scoreLogicFunction.txt/scoreLogicFunction.jsのフルロジック(885行)を全走査していないため、個別の減点・加点アルゴリズムやhiyari判定の具体条件は業務ドキュメント(運転診断アプリ_詳細設計書_20250228)側に依存。3) スコアメッセージのidと`custom`列の対応表(例: 200番台=減速度評価等)はJSONの全レンジを読み切っていないため確度中。4) BLE車載機の実物・提供元、Google Maps APIキーの管理主体、apkの配布経路は不明。5) build-android.bat等のビルドスクリプトを未読のためリリースパッケージング詳細は保留。6) editPageの一部(グラフ描画バッファ操作)は複雑で挙動を全パス確認できていない。7) パスワードのソルト無しMD5、APIキーの平文埋込は技術負債の可能性があるがunknownsへは業務判断の観点で1件のみ含めた。

The following nodes were inferred with varying confidence:

- **app.bootstrap.module** (app): Ionic/Angularアプリケーションのルートモジュール。BrowserModule、IonicModule、IonicStorageModule、HttpClientModuleを取り込み、ScreenOrientation/Geolocation/DeviceMotion/Magnetometer/SQLite/AndroidPermissionsといったCordovaプラグインをDI用にProvider登録する。IonicRouteStrategyを使うことでionicのページキャッシュ挙動を有効化。
- **app.bootstrap.component** (app): ルートAngularコンポーネント。テンプレート/スタイルのみを保持しロジックは持たない。ionicのrouter-outletコンテナとして機能。
- **app.routing** (app): ページ遷移定義。空パスは/openingへリダイレクト。account(:type)、bad-spot(:path)、comment、driving、history、opening、settings、editの各ページをlazy-loadで登録。PreloadAllModules戦略で事前ロードする。
- **ui.page.opening** (ui): オープニング（トップ）ページ。起動時にAndroidパーミッションを一括要求し、LoginService/ScoreDbService/LogServiceの初期化、Google Maps JSプリロード、assetsのscoreLogicFunction.txt/scoreLogic.jsonを内部ストレージへ初期値として保存する。自動ログイン(3日間有効)を試み、成功時は最後の運転診断のover_allコメントをダイアログ表示する。ログイン/ログアウト/運転診断/アカウント作成・編集/設定への導線を持つ。
- **ui.page.driving** (ui): 運転診断メインページ。SensorServiceからGPS/加速度/ジャイロ/磁気/CANデータを受け、ScoreLogicで診断を定期実行、Google Mapsに現在位置・走行軌跡・ヒヤリ地点マーカーを描画。設定によりMediaRecorderで運転動画を60秒ごとにファイル出力し、Insomniaで画面スリープを抑止する。スコアは総合(over_all)+4指標(score1〜4)を平均で更新表示。終了時にScoreDbServiceでDBへ保存し、scoreIdをLoginServiceに退避する。ヒヤリ地点タップでbad-spotページへ遷移。
- **ui.page.history** (ui): 過去の運転診断履歴ページ。ScoreDbServiceで最大30件の履歴を取得し、Chart.jsで総合スコアの折れ線グラフ・4指標のスコアグラフ・3つの能力指標(scoreA/B/C)グラフを描画。「ヒストリー」と「能力指標」の2タブ構成。ラベル文字列と得点/順位表示切替(scoreShowStar)はLoginServiceのsettingsに従う。過去N日(capability_score_target_days)以内の能力指標の平均を総合値としてサマリ表示。
- **ui.page.comment** (ui): 直近の運転診断結果に対するアドバイス表示ページ。指標ごと(over_all/score1〜4)にpositive/negativeメッセージから代表を選出(positiveは最大値、negativeは最小値のスコアを持つメッセージ)し、%COUNT/%INTERSECTIONを差し替えて表示。orderOfMessage設定でpositive優先/negative優先の順序を制御する。
- **ui.page.badSpot** (ui): ヒヤリ地点詳細ページ。運転診断で登録されたマーカー位置をGoogle Mapsで拡大表示し、対応する運転動画(webm)を該当秒からシークして再生。前/次ボタンで隣接するヒヤリ地点へ移動、コメントは4指標分を表示。動画の再生時刻とマーカー位置は同期する。
- **ui.page.account** (ui): アカウント作成/編集/削除ページ。ルートパラメータtypeで挙動を切替。ユーザーID(英数字)、パスワード(MD5ハッシュ化)、性別/生年月日(年月)、身長(cm)、都道府県(47区分)を入力。バリデーション後LoginService経由でSQLite usersテーブルへinsert/update/deleteし、削除時はScoreDbServiceで関連スコアも削除する。
- **ui.page.settings** (ui): アプリ設定ページ。動画録画/GPSデモ/ログ保存/センサーログ保存の各ON/OFF、センサーモード(smartphoneOnly/canDataOnly/combination)を切替。運転診断ロジック(JS文字列)と設定JSONのアップロード/端末保存/ダウンロード、スコアロジックのエディタ画面(/edit)への遷移が可能。アップロード時はScoreLogic.testScoreLogicで実行可能性を検証してから保存する。
- **ui.page.edit** (ui): スコアロジック編集/検証ページ。センサーログ(.gz)と動画(.webm)をアップロードし、テキストエディタでスコアロジックJSを編集、DemoDataから疑似センサーを流してScoreLogicを実行しChart.jsで多系列グラフ描画(GPS/加速度/ジャイロ/磁気/CAN/スコア/能力指標/ヒヤリ)。ユーザーが指定するY1/Y2軸レンジ、系列表示のON/OFF、動画同期再生、シークバー、一時停止/再開に対応。保存時は自動でヘッダ行にタイムスタンプを差し込む。
- **service.sensor** (service): スマートフォンの各種センサー(Capacitor Geolocation.watchPosition、window devicemotion、deviceorientationabsolute、Cordova Magnetometer)を購読し、10msタイマで最新値スナップショットを合成してリスナに渡すサービス。BLEDeviceを介して車載CANデータ(11バイト固定フォーマット)も統合。runScoreLogicフラグがtrueの時のみセンサーコールバックを処理する。ブラウザ環境ではDemoDataのセンサーログを疑似的に流し、100msに1回だけ地図更新用にリスナへ通知する。取得したGPSの最終位置はストレージへ保存し、次回起動時のマップ中心に使う。
- **service.login** (service): 認証と設定の集約サービス。UserDbServiceを介してSQLiteのusersテーブルとハッシュ済みパスワードで照合、自動ログインは3日間の有効期限で判定する。ローカルストレージからアプリ設定(録画/GPSデモ/ログ保存/センサーログ保存/センサーモード)を復元し、scoreLogicJsonのsettings(order_of_message、label1〜4/A〜C、score_logic_interval_ms、capability_score_target_days、score_show_star)を読み込んでプロセス内で共有する。scoreIdは診断終了直後の詳細画面連携に使う一時値。
- **service.userDb** (service): SQLite(driving-score.db)のusersテーブルCRUDを担当。ID(TEXT PRIMARY KEY)、パスワード、性別、生年月、身長、都道府県を保持。ブラウザ環境ではUser.dummy()を返しモックとして機能する。
- **service.scoreDb** (service): SQLite(driving-score.db)のscore/score_history/capability_scoreテーブルCRUD。運転診断終了時にScoreLogicの状態から総合と4指標、履歴メッセージ、能力指標を1000件単位で分割INSERTする。参照時はJOINで能力指標とメッセージを一括取得しScoreインスタンスに集約。ブラウザ環境ではランダムなダミーデータを最大30件返し、UI検証に使う。ユーザー削除時は関連スコアも連鎖削除。
- **service.log** (service): デバッグログとセンサーログをローテーション付きでpakoによりgzip圧縮しAndroid外部ストレージ(Documents/driving-score/debug-log/、および運転診断毎のdata.YYYYMMDD-hhmmss/)に書き出すサービス。デバッグログは3MB、センサーログは5MB、または明示のforceで書き出す。ブラウザ環境では無効化。運転診断ごとに一時ディレクトリを切り替える(setLogDir/resetLogDir)。
- **service.map** (service): Google Maps JS APIのラッパーサービス。@googlemaps/js-api-loaderで日本語/JP地域固定でロード。自車マーカー(青矢印)、開始/終了マーカー、走行軌跡の点マーカー(最大1000件でFIFO削除)、ヒヤリ地点マーカー(通常/選択時大アイコン)を描画。マップドラッグで自車追従を無効化するリスナ登録、可視領域外マーカーの非表示化を行う。
- **domain.scoreLogic** (domain): 運転診断ロジック実行エンジン。ローカルストレージに保存された動的JS文字列を`new Function(...)`で関数化し、設定インターバル(既定300ms)ごとにGPS/加速度/ジャイロ/磁気/CAN/前回結果/設定JSON/ローカルデータ/ログ/描画コールバックを渡して実行。戻り値からScoreインスタンスを組み立て、over_all/score1〜4/hiyari/capabilityScore/messagesの平均を都度計算する。開始時にsensorStockTime(60秒)分の古いセンサーデータを都度パージする。ScoreLogic.testScoreLogicで文字列ロジックのシンタックス/実行可否を代表データで事前検証できる。
- **domain.sensorManager** (domain): センサーキャリブレーションと座標変換のstaticユーティリティ。開始から100ms経過後の1秒分の加速度/磁気平均をオフセット化し、Android SensorManager互換のgetRotationMatrix/remapCoordinateSystem/getOrientationでピッチ・ロールを算出。ローパスフィルタ(τ=1/(2πF), F=0.2Hz, T=0.0167s)で高周波ノイズを除去し、端末を縦持ちした場合の座標系に加速度・ジャイロを回転補正する。
- **domain.ble** (domain): BLEデバイス(名前:DrivingCanData、サービスUUID 0x2310、Notify特性 0x2311)への接続クラス。3秒間のScan後に一意のデバイスがあれば自動接続、複数あればラジオボタン選択ダイアログを表示。Notifyで到着するArrayBufferをコールバックへ渡す。失敗時はAlertControllerでリトライダイアログを表示する。

Items in `reverse_unknowns.md` require stakeholder confirmation.
