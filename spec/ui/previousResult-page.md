<!-- 作成: 2026-09-18 18:39:04 JST -->

# 1-2 前回結果表示画面（診断開始前画面） 仕様

- **ノード ID**: `ui.previousResult.page`
- **エントリポイント**: `spec/ui/previousResult-page.md`
- **画面番号**: 1-2（診断開始前画面 / 前回結果表示）
- **改修区分**: 2026 年度改修要求 ①「診断開始前画面の前回結果表示」に対応する新規画面

---

## 1. 目的と位置づけ

診断を開始する前に、利用者が自身のこれまでの運転傾向を振り返れるようにする画面である。
本画面は「直近 1 回の結果」ではなく **過去の履歴の平均** を提示することを基本方針とする。これは、1 回の診断結果のばらつきに利用者が過剰反応することを避け、傾向として自身の状態を把握できるようにするためである。

本画面は 2026 年度改修要求 5 本のうち、以下に関係する。

- ① 診断開始前画面の前回結果表示（本画面の新設）
- ② タブ切り替えの追加（本画面を含むタブ構成。切替先の確定は後述の未確定事項）
- ③ 採点スコアのレーダーチャート表示（本画面に描画するチャート仕様）

---

## 2. 画面構成

本画面は以下の 2 つの表示領域で構成される。

| 領域 | 内容 |
|---|---|
| レーダーチャート領域 | 過去の履歴平均によるレーダーチャート |
| 地図領域 | 過去のヒヤリポイントをマーカー表示する地図 |

---

## 3. レーダーチャート表示仕様

### 3.1 表示項目

レーダーチャートは以下の 6 項目で構成する。

1. 筋力
2. 柔軟性
3. 空間把握
4. 危険予測
5. 視力
6. 視野

各項目には、いらすとやの素材によるアイコンを添えて表示する。

### 3.2 尺度

- 各項目の評価値は **1〜5 の 5 段階** とする。
- 5 に近いほど良好である。
- 3 が年齢の平均を表す。

### 3.3 併記する情報

各項目に対して、以下を併記する。

- 「n 点（5 点満点）」形式の得点表示
- 2 行程度の評価コメント

### 3.4 描画系列

チャートには 2 系列を図示する。

- **今回**
- **過去の平均**

ただし本画面（1-2）は診断開始前に表示される画面であるため、本画面における「前回結果」としての描画対象は **過去の履歴の平均** である。「今回」系列を本画面で併記するか否か（診断未実施時の扱いを含む）は、後述の未確定事項として扱う。

### 3.5 履歴が存在しない場合

- 初期起動など、過去の記録が 1 件も存在しない場合は **レーダーチャートの線を描画しない**。
- 軸・項目ラベル・アイコンといったチャートの枠自体を表示するか否かについては、線を描かないという要件のみが確定事項であり、枠の表示可否は実装判断に委ねる。

### 3.6 従来のスコア表示との関係

新仕様では従来の ☆ によるスコア表示を削除し、レーダーチャートと評価コメントによる表示に置き換える。
先方資料には「レーダーチャートを押すと 5-1 に遷移」との記載があるが、現行実装では診断終了後のスコア欄タップが 5-1（アドバイス表示 / `ui.comment.page`）への唯一の導線であるため、削除後の導線定義は未確定である（第 6 章参照）。

---

## 4. 地図・ヒヤリポイント表示仕様

### 4.1 マーカー表示

- 地図上に、過去のヒヤリポイントを **マーカーとして表示** する。

### 4.2 マーカータップ時の動作

マーカーをタップした際の動作は **従来通り** とする。すなわち、

- 該当ヒヤリの録画動画を再生する
- 発生日時を表示する
- 評価コメントを表示する

### 4.3 表示件数

- 表示するヒヤリポイントを直近何回分に限るかは **検討中** である。
- 全件を表示するとマップがマーカーで埋まるため、件数制限が検討されている。
- 件数制限の値が確定するまでは、件数上限を設定値として外出しできる構造を想定する（値そのものは未確定）。

### 4.4 動画再生方式に関する注意

ヒヤリ動画は改修により「ヒヤリ前後 15 秒の個別動画」へ変更される可能性がある。
現行 6-1（ヒヤリシーン表示）は診断全体の通し動画に対し 300ms 周期で `currentTime` を監視してマーカーを追尾する方式であるため、本画面のマーカータップから起動する再生方式も影響を受ける。方式確定までは、本画面側は「マーカー ⇔ 該当ヒヤリ動画」の対応関係のみを責務とし、再生の内部方式には依存しない設計とする。

---

## 5. データの扱い

- 本画面は **表示責務のみ** を持つ。レーダーチャートの各項目値および評価コメントの算出は本画面の責務外とする。
- 「過去の履歴の平均」の集計は本画面では行わず、算出済みの値を受け取って描画する。
- 履歴が 0 件である旨の判定結果も、本画面は受け取った状態に従って描画を切り替える。

---

## 6. 未確定事項（本画面に影響するもの）

| # | 内容 | 影響 |
|---|---|---|
| 1 | 履歴平均の集計期間。現行は `scoreLogic.json` の `capability_score_target_days`（既定 30 日）で能力指標を平均しているが、これを流用するか別定義を置くかが未確定 | チャートが示す「前回」の意味が変わる |
| 2 | 地図に表示する過去ヒヤリの件数上限 | マーカー密度・可読性 |
| 3 | 6 項目の採点データ形式（スコア／評価メッセージの受け取り方）。先方で検討中、試作中に提示予定 | チャート描画・コメント表示の入力仕様 |
| 4 | 能力指標の保持構造。現行 `capability_score` は scoreA/B/C の 3 列・0〜100 の REAL であり、6 項目・1〜5 の新仕様と列数も尺度も一致しない | 表示値の生成元 |
| 5 | 従来の ☆ スコア表示削除後の 5-1（アドバイス表示）への導線。「レーダーチャートを押すと 5-1 に遷移」という記載の採否を含む | 本画面からの遷移仕様 |
| 6 | 画面の縦横変更要求。現行は Android で PORTRAIT 固定であり、履歴画面・アドバイス画面で `screenOrientation.lock` が掛かっている | 本画面を回転対応にするか、横向きレイアウトをどう構成するか |
| 7 | タブ構成。要求②「タブ切り替えの追加」は有効だが、切替先として例示されていた 8-1 サービス案表示（`ui.servicePlan.page`）は本改修の対象外（後続検証）とする裁定が下りている | 本改修で実装するタブの本数と切替先 |
| 8 | ヒヤリ動画の個別化にともなう再生方式（マーカーとの対応づけ、前後ボタンの挙動を含む） | マーカータップ後の挙動 |

---

## 7. 対象外

- 8-1「サービス案表示」画面（`ui.servicePlan.page`）のノード新設は取り下げられており、**本改修の対象外（後続検証）** である。本画面からサービス案画面への遷移は本改修では実装しない。

---

## 8. 制約

- アプリ開発は **2026 年 11 月末までの完了** を目標とする（2026 年 12 月より高齢者を招いた実験が開始されるため）。

```json
{
  "required_changes": [
    {"node": "ui.previousResult.page", "entrypoint": "spec/ui/previousResult-page.md", "description": "診断開始前画面 1-2 を新設し、履歴平均のレーダーチャートと過去ヒヤリポイントの地図表示を規定"},
    {"node": "ui.previousResult.page", "entrypoint": "spec/ui/previousResult-page.md", "description": "レーダーチャートを6項目・1〜5の5段階・今回/過去平均の2系列で描画し、各項目に得点と2行程度の評価コメントを併記する仕様を追加"},
    {"node": "ui.previousResult.page", "entrypoint": "spec/ui/previousResult-page.md", "description": "履歴が存在しない場合はレーダーチャートの線を描画しない条件を明記"},
    {"node": "ui.previousResult.page", "entrypoint": "spec/ui/previousResult-page.md", "description": "地図上の過去ヒヤリポイントのマーカータップ動作を従来通り（動画再生・発生日時・評価コメント表示）と規定"},
    {"node": "ui.previousResult.page", "entrypoint": "spec/ui/previousResult-page.md", "description": "8-1 サービス案表示は本改修の対象外である旨を明記し、タブ切替先の未確定を open_question として記載"}
  ],
  "suggested_impacts": [
    {"domain": "Middleware-agent", "severity": "must", "reason": "本画面は表示責務のみを持つため、履歴平均（6項目・1〜5段階）と評価コメントの算出・提供がMiddleware側に必要"},
    {"domain": "Middleware-agent", "severity": "must", "reason": "履歴平均の集計期間（capability_score_target_days の流用可否）の決定がチャート表示値に直結する"},
    {"domain": "DB-agent", "severity": "must", "reason": "現行 capability_score は scoreA/B/C の3列・0〜100 REAL で、6項目・1〜5 の新尺度と不一致のため保持構造の見直しが必要"},
    {"domain": "Middleware-agent", "severity": "should", "reason": "地図に表示する過去ヒヤリの件数上限を設定値として外出しする場合、取得APIに件数指定が必要"},
    {"domain": "UI-agent", "severity": "should", "reason": "ui.comment.page（5-1）への導線が従来のスコア欄タップから変更されるため、遷移仕様の再定義が必要"},
    {"domain": "UI-agent", "severity": "should", "reason": "画面の縦横変更要求は現行の PORTRAIT 固定（screenOrientation.lock）と衝突し、本画面の回転対応可否に影響する"},
    {"domain": "Middleware-agent", "severity": "should", "reason": "ヒヤリ動画が個別動画化された場合、マーカーから該当動画を特定する対応づけの提供方式が変わる"}
  ],
  "requirements_context": "1-2 診断開始前画面（ui.previousResult.page）は2026年度改修要求①に対応する新規画面。日産自動車受領の2資料（『運転機能チェックアプリの一次仕様』2026-08-04、メイサンソフト『要求仕様確認』2026-09-17）に基づく。画面は(a)レーダーチャート領域と(b)過去ヒヤリポイントを示す地図領域で構成する。レーダーチャートは筋力・柔軟性・空間把握・危険予測・視力・視野の6項目、1〜5の5段階（5に近いほど良好、3が年齢平均）で、各項目に『n点（5点満点）』と2行程度の評価コメントを併記し、いらすとやのアイコンを添える。系列は『今回』と『過去の平均』の2系列。本画面が示す『前回』は直近1件ではなく過去の履歴の平均であり、記録が存在しない場合（初期起動等）は線を描画しない。地図は過去のヒヤリポイントをマーカー表示し、タップ時の動作は従来通り（録画動画再生、発生日時表示、評価コメント表示）。表示件数は全件だとマップが埋まるため直近n回分への制限を検討中で未確定。本画面は表示責務のみを持ち、平均値算出やコメント生成はMiddleware側責務。従来の☆スコア表示は削除されレーダーチャートと評価コメントに置換されるが、5-1（ui.comment.page）への導線が未定義。8-1サービス案表示（ui.servicePlan.page）はノード新設が取り下げられ本改修の対象外（後続検証）であり、要求②のタブ切替先が未確定。画面の縦横変更要求は現行のPORTRAIT固定と衝突。能力指標の保持構造（現行 capability_score は scoreA/B/C の3列・0〜100 REAL）は新尺度と不一致。履歴平均の集計期間（現行 capability_score_target_days 既定30日）の扱いも未確定。6項目の採点データ形式は先方検討中で試作中に提示予定。ヒヤリ動画の個別化（前後15秒）にともなう再生方式も未確定。開発は2026年11月末完了目標（12月から高齢者実験開始）。",
  "fact_candidates": [
    {"type": "display_rule", "title": "1-2 画面はレーダーチャートと地図の2領域で構成される", "statement": "診断開始前画面 1-2 は、履歴平均を示すレーダーチャート領域と、過去のヒヤリポイントをマーカー表示する地図領域で構成される", "status": "candidate"},
    {"type": "display_rule", "title": "レーダーチャートは6項目・1〜5の5段階で表示される", "statement": "1-2 のレーダーチャートは筋力・柔軟性・空間把握・危険予測・視力・視野の6項目を1〜5の5段階（5に近いほど良好、3が年齢平均）で表示する", "status": "candidate"},
    {"type": "display_rule", "title": "各項目に得点と評価コメントを併記する", "statement": "1-2 のレーダーチャートは各項目に『n点（5点満点）』の得点と2行程度の評価コメントを併記して表示する", "status": "candidate"},
    {"type": "display_rule", "title": "各項目にいらすとやのアイコンを添える", "statement": "1-2 のレーダーチャート各項目には、いらすとやの素材によるアイコンを添えて表示する", "status": "candidate"},
    {"type": "display_rule", "title": "レーダーチャートは今回と過去平均の2系列を図示する", "statement": "レーダーチャートは『今回』と『過去の平均』の2系列を図示する", "status": "candidate"},
    {"type": "display_rule", "title": "1-2 の『前回』は履歴平均である", "statement": "1-2 が表示する『前回結果』は直近1件ではなく過去の履歴の平均である", "status": "candidate"},
    {"type": "state_rule", "title": "履歴が無い場合はチャートの線を描画しない", "statement": "過去の記録が1件も存在しない場合、1-2 のレーダーチャートは線を描画しない", "status": "candidate"},
    {"type": "display_rule", "title": "地図に過去のヒヤリポイントをマーカー表示する", "statement": "1-2 の地図上に過去のヒヤリポイントをマーカーとして表示する", "status": "candidate"},
    {"type": "display_rule", "title": "マーカータップ時の表示内容は従来通り", "statement": "1-2 の地図上のヒヤリポイントマーカーをタップすると、録画動画の再生、発生日時、評価コメントが表示される", "status": "candidate"},
    {"type": "input_rule", "title": "1-2 の表示値は利用者が編集できない", "statement": "利用者は 1-2 上でレーダーチャートの得点・評価コメント・ヒヤリポイント情報を編集してはならない", "status": "candidate"},
    {"type": "display_rule", "title": "従来の☆スコア表示は削除される", "statement": "新仕様では従来の☆によるスコア表示を削除し、レーダーチャートと評価コメントによる表示に置き換える", "status": "candidate"},
    {"type": "constraint", "title": "1-2 は表示責務のみを持つ", "statement": "1-2 は算出済みの値を受け取って描画する表示責務のみを持ち、履歴平均の算出および評価コメントの生成は行わない", "status": "candidate"},
    {"type": "constraint", "title": "8-1 サービス案表示は本改修の対象外", "statement": "8-1 サービス案表示（ui.servicePlan.page）はノード新設が取り下げられ本改修の対象外（後続検証）であり、1-2 からサービス案画面への遷移は本改修では実装しない", "status": "candidate"}
  ],
  "open_questions": [
    "履歴平均の集計期間が未確定。現行 scoreLogic.json の capability_score_target_days（既定30日）を流用するか別定義を置くかが資料から判断できず、Middleware/DBの判断が必要。決まらないとチャートが示す『前回』の意味と表示値が確定しない。",
    "地図に表示する過去ヒヤリポイントの件数上限が未確定。全件表示するとマップがマーカーで埋まるため件数制限が検討中。UI/先方の判断が必要で、決まらないと地図の可読性と取得APIの件数指定仕様が確定しない。",
    "6項目の採点データ形式（スコアおよび評価コメントの受け取り方）が未確定。先方で検討中・試作中に提示予定であり、Middleware/先方の判断が必要。決まらないとチャート描画とコメント表示の入力仕様が確定しない。",
    "能力指標の保持構造が新仕様と不一致。現行 capability_score は scoreA/B/C の3列・0〜100 REAL で、6項目・1〜5 の5段階と列数も尺度も合わない。DBドメインの判断が必要で、決まらないと表示値の生成元が確定しない。",
    "従来の☆スコア表示削除後の 5-1（ui.comment.page）への導線が未定義。現行はスコア欄タップが唯一の導線であり、先方資料は『レーダーチャートを押すと 5-1 に遷移』とも記載。UIドメインの判断が必要で、決まらないとアドバイス画面に到達できなくなる。",
    "1-2 は診断開始前に表示される画面であるため、レーダーチャートに『今回』系列を併記するのか、過去平均のみを描画するのかが未確定。UI/先方の判断が必要で、決まらないと診断未実施時の描画内容が確定しない。",
    "画面の縦横変更要求が現行の縦固定と衝突。現行は Android で PORTRAIT 固定（screenOrientation.lock）であり、1-2 を回転対応にするか、横向きレイアウトをどう構成するかが未確定。UIドメインの判断が必要。",
    "要求②『タブ切り替えの追加』の切替先が未確定。切替先として例示されていた 8-1 サービス案表示が本改修の対象外となったため、本改修で実装するタブの本数と切替先が定まらない。",
    "ヒヤリ動画の個別化（前後15秒）にともなう再生方式が未確定。現行 6-1 は通し動画に対し300ms周期で currentTime を監視する方式であり、個別動画化された場合のマーカーとの対応づけ・前後ボタン挙動が未定。Middleware/UIの判断が必要。",
    "履歴が0件の場合にレーダーチャートの枠（軸・項目ラベル・アイコン）を表示するか否かが未確定。確定事実は『線を描画しない』のみ。"
  ],
  "rationale_notes": [
    "『前回』を直近1件ではなく履歴平均と定義したのは、1回の診断結果のばらつきに利用者が過剰反応することを避け、傾向として自身の状態を把握させるためと解釈している。",
    "1-2 は表示責務のみを持たせ、平均算出・スコア生成・コメント生成は Middleware 側に置く方針とした。これにより 6 項目の採点データ形式が未確定でも UI 側の構造を先行して固められる。",
    "マーカータップ後の動画再生については、個別動画化の方式が未確定なため、1-2 側は『マーカー ⇔ 該当ヒヤリ動画』の対応関係のみを責務とし再生の内部方式に依存しない設計とした。",
    "ヒヤリ件数上限は値が未確定のため、設定値として外出しできる構造を想定している。値そのものは仕様として断定していない。",
    "8-1 サービス案表示は design_decision により本改修対象外と裁定されているため、1-2 側の仕様からも遷移を除外した。ただし要求②のタブ切り替え自体は approved fact として残るため、タブ構成は open_question として明示した。",
    "2026年11月末完了・12月実験開始という期限制約があるため、未確定事項が多い領域（採点データ形式、縦横対応、動画再生方式）は UI 側の構造を疎結合に保ち、後から差し替え可能にしておく意図がある。"
  ]
}
```