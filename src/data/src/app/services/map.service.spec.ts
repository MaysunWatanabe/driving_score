// Angular CLI generated stub. MapService depends on the Google Maps loader
// which cannot be initialized in the karma runner without a valid key.
// Placeholder per ProjectSmith proposal #584 scope.
describe('MapService (placeholder)', () => {
  it('smoke', () => {
    expect(true).toBe(true);
  });
});
