// Angular CLI generated stub. SensorService has DI dependencies (LogService,
// AlertController, ...) that are not provided in the auto-generated TestBed
// module, so `TestBed.inject(SensorService)` throws at runtime. Placeholder
// per ProjectSmith proposal #584 scope.
describe('SensorService (placeholder)', () => {
  it('smoke', () => {
    expect(true).toBe(true);
  });
});
