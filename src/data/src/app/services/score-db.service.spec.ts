// Angular CLI generated stub. ScoreDbService requires SQLite bindings that
// are not available in karma. Placeholder per ProjectSmith proposal #584 scope.
describe('ScoreDbService (placeholder)', () => {
  it('smoke', () => {
    expect(true).toBe(true);
  });
});
