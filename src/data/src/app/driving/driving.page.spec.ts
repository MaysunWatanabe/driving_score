// Angular CLI generated stub. DrivingPage pulls in Google Maps loader, BLE
// plugin and sensor services that are not usable in karma. Placeholder per
// proposal #584 scope.
describe('DrivingPage (placeholder)', () => {
  it('smoke', () => {
    expect(true).toBe(true);
  });
});
