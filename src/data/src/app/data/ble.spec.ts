// Angular CLI generated stub. Original tried `new BLEDevice()` but the class
// requires constructor args and the auto-import was wrong (imported `Score`).
// Kept as compile-passing placeholder per ProjectSmith proposal #584 scope
// ("既存 unit spec を CI で回す") until spec/qa シナリオが実装される。
describe('BLEDevice (placeholder)', () => {
  it('smoke', () => {
    expect(true).toBe(true);
  });
});
